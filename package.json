{
  "name": "growup-agency-website",
  "private": true,
  "scripts": {
    "build": "npm --prefix growup-agency ci --cache ./.npm-cache && npm --prefix growup-agency run build",
    "dev": "npm --prefix growup-agency run dev"
  }
}
