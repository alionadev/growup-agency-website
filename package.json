{
  "name": "growup-agency-website",
  "private": true,
  "scripts": {
    "build": "MONOREPO_ROOT_BUILD=1 npm --prefix growup-agency ci --cache ./.npm-cache && MONOREPO_ROOT_BUILD=1 npm --prefix growup-agency run build",
    "dev": "npm --prefix growup-agency run dev"
  }
}
